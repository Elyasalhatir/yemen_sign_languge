// Configuration for the application
const CONFIG = {
    // Empty string uses same-origin (works for both local and production)
    // The app will automatically use the current domain
    API_URL: ''
};

// Export for use in other files
window.AppConfig = CONFIG;
